query GetServiceBlockById($id: ID!) {
  servicesBlock_by_id(id: $id) {
    id
    title
    description

    desc

    img {
      ...MediaFragment
    }
    allServices {
      ...ServiceFragment
    }
  }
}
